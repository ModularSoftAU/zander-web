<%- include('../header.ejs', {
    pageTitle: pageTitle,
    pageDescription: "Notifications"
}) %>

<%- include('../navigationBar.ejs') %>

<%- include('../../partials/miniHeader.ejs', {
    headerTitle: "Notifications",
    globalImage,
}) %>

<div class="container">
    <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center flex-wrap gap-2">
            <h2 class="mb-0">Notifications</h2>
            <span class="badge bg-primary">Unread: <%= unreadCount %></span>
        </div>
        <div class="list-group list-group-flush">
            <% if (notifications && notifications.length) { %>
                <% notifications.forEach((notification) => { %>
                    <a
                        href="/notifications/visit/<%= notification.notificationId %>"
                        class="list-group-item list-group-item-action d-flex flex-column gap-2 <%= notification.isRead ? '' : 'bg-light' %>"
                    >
                        <div class="d-flex justify-content-between align-items-center">
                            <div class="fw-semibold"><%= notification.title %></div>
                            <% if (!notification.isRead) { %>
                                <span class="badge bg-primary">New</span>
                            <% } %>
                        </div>
                        <div class="text-muted"><%= notification.message %></div>
                        <small class="text-muted"><%= new Date(notification.createdAt).toLocaleString() %></small>
                    </a>
                <% }) %>
            <% } else { %>
                <div class="list-group-item text-muted">You're all caught up.</div>
            <% } %>
        </div>
    </div>
</div>
