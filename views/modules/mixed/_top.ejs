<%- include("../../modules/header.ejs", {
    pageTitle: pageTitle,
    pageDescription: typeof pageDescription !== 'undefined' ? pageDescription : 'Mixed'
}) %>

<%- include("../../modules/navigationBar.ejs") %>

<%- include("../../partials/miniHeader.ejs", {
    headerTitle: pageTitle || "Mixed",
    globalImage,
}) %>

<%
  const mixedNav = [
    ["Live", "/mixed/live"],
    ["Matches", "/mixed/matches"],
    ["Maps", "/mixed/maps"],
    ["Players", "/mixed/players"],
    ["Leaderboards", "/mixed/leaderboards"],
  ];
  const _path = req.url.split('?')[0];
%>

<section class="section pt-4 pb-5">
  <div class="container">
    <div class="mb-3">
      <div class="mixed-section-label">Mixed</div>
    </div>
    <ul class="nav nav-pills mixed-tabs mb-4">
      <% mixedNav.forEach(function(item){ %>
        <li class="nav-item">
          <a class="nav-link mixed-tab-link py-1 px-3 <%= _path === item[1] || (item[1] !== '/mixed' && _path.startsWith(item[1])) ? 'active' : '' %>"
             href="<%= item[1] %>"><%= item[0] %></a>
        </li>
      <% }) %>
    </ul>
